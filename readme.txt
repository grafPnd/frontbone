 prefix r_ in classnames is used for styles only in redesigned layout 
 prefix s_ in classnames is used for adding static common styles 
 prefix j_ in classnames and id is used ONLY for DOM selector, not for styles 
 